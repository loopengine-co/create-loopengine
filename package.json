{
  "name": "create-loopengine",
  "version": "0.1.35",
  "description": "Scaffold a new loopengine agent project — a runnable HTTP server with one example agent, in your own repo.",
  "license": "MIT",
  "type": "module",
  "homepage": "https://loopengine.co",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/evanyan22/create-loopengine.git"
  },
  "bin": {
    "create-loopengine": "dist/cli.js"
  },
  "files": [
    "dist",
    "template",
    "LICENSE"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc && chmod +x dist/cli.js",
    "prepublishOnly": "npm run build",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
